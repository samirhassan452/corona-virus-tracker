// hold the key we need when we request access token with the rest api
class APIKeys {
  // SandboxKey that we use when requesting access token
  // put your api_key inside the double quotes
  static String ncovSandboxKey = "";
}
